# Core inference
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0,<2.0
Pillow>=10.0.0
huggingface_hub>=0.20.0

# VGGT (install from source: pip install -e /path/to/vggt)
# git+https://github.com/facebookresearch/vggt.git

# Computer vision
opencv-python>=4.8.0

# ROS2 Python bindings (installed via apt, not pip)
# rclpy, sensor_msgs, geometry_msgs, nav_msgs, tf2_ros, cv_bridge

# Optional: point cloud downsampling
open3d>=0.17.0
